# Native CI problems reports

This directory stores the latest Gradle problems report generated by the
`Build Native Android APKs` workflow when the manual input
`persist_problem_report` is set to `true` and the build fails.

Current latest report path:
- `latest/problems-report.html`
