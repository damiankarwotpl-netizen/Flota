# Android native migration backlog

## Remaining steps to full migration

1. Port contact/worker/plant CRUD persistence and sync rules end-to-end.
2. Port car workflows end-to-end, including driver-account API sync and mileage background sync.
3. Implement full vehicle-report PDF generation parity for admin and driver apps.
4. Port workbook import, payroll import, table preview, and export logic.
5. Port SMTP, attachments, special-send flow, mass mailing, and session reporting.
6. Port the full clothes module with sizes, order wizard, issue flows, CSV/XLSX exports, and yearly statistics.
7. Add real remote API wiring, notifications, background work, QA, and release hardening.

## Current count
**7 major implementation steps remain** after wiring containers/repositories/viewmodels into the Android apps.
